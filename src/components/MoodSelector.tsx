
import { useChat } from "@/context/ChatContext";
import { cn } from "@/lib/utils";
import { useIsMobile } from "@/hooks/use-mobile";

// Empty component as we're removing mood selection functionality
const MoodSelector = () => {
  return null;
};

export default MoodSelector;
